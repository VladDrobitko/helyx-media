# 🚀 Настройка Supabase для HELYX Media

## 📋 Шаг 1: Создание проекта Supabase

1. **Перейдите на** [supabase.com](https://supabase.com)
2. **Нажмите** "Start your project" → "New project"
3. **Заполните**:
   - **Name**: `helyx-media`
   - **Password**: придумайте надежный пароль для БД
   - **Region**: выберите ближайший (Europe West для вас)
4. **Нажмите** "Create new project"
5. **Ждите** 2-3 минуты пока проект создается

## 📋 Шаг 2: Получение ключей

1. **В боковом меню** нажмите "Settings" → "API"
2. **Скопируйте**:
   - **Project URL** (например: `https://abcdefgh.supabase.co`)
   - **anon public key** (длинный ключ начинающийся с `eyJ...`)

## 📋 Шаг 3: Настройка Environment Variables в Vercel

1. **Откройте** [vercel.com](https://vercel.com) → Ваш проект `helyx-media`
2. **Нажмите** "Settings" → "Environment Variables"
3. **Добавьте переменные**:

   | Name | Value | Environment |
   |------|-------|-------------|
   | `VITE_SUPABASE_URL` | `https://your-project.supabase.co` | All |
   | `VITE_SUPABASE_ANON_KEY` | `eyJ...` (ваш anon key) | All |
   | `VITE_ADMIN_PASSWORD` | `your-secure-password` | All |

4. **Нажмите** "Save" для каждой переменной

## 📋 Шаг 4: Создание таблиц в базе данных

1. **В Supabase** нажмите "SQL Editor" в боковом меню
2. **Нажмите** "New query"
3. **Скопируйте весь код** из файла `supabase-setup.sql`
4. **ВАЖНО**: Замените `'your-admin@email.com'` на ваш настоящий email
5. **Нажмите** "Run" для выполнения скрипта

## 📋 Шаг 5: Настройка Storage

### Storage уже создается автоматически скриптом, но проверьте:

1. **Перейдите** в "Storage" в боковом меню
2. **Должны быть созданы 2 bucket'а**:
   - `videos` - для видеофайлов
   - `thumbnails` - для превью изображений

### Если bucket'ы не созданы:

1. **Нажмите** "Create bucket"
2. **Создайте** bucket с именем `videos`:
   - Name: `videos`
   - Public bucket: ✅ **Включено**
3. **Повторите** для bucket'а `thumbnails`

## 📋 Шаг 6: Настройка аутентификации (для админа)

1. **Перейдите** в "Authentication" → "Users"
2. **Нажмите** "Add user"
3. **Заполните**:
   - **Email**: тот же email что указали в SQL скрипте
   - **Password**: придумайте пароль для входа в админку
   - **Email Confirm**: ✅ **Отключите** "Email confirmation"
4. **Нажмите** "Create user"

## 📋 Шаг 7: Деплой и тестирование

1. **В Vercel** нажмите "Deployments" → "Redeploy" для перезапуска с новыми переменными
2. **Откройте** `https://your-site.vercel.app/admin`
3. **Войдите** с паролем из `VITE_ADMIN_PASSWORD`
4. **Проверьте** что видите реальные данные вместо демо

## 🎬 Теперь вы можете:

- ✅ **Загружать реальные видео** через админку
- ✅ **Управлять портфолио** из интерфейса
- ✅ **Получать заявки** через контактную форму
- ✅ **Публиковать/скрывать** работы
- ✅ **Отмечать Featured** для главной страницы

## 🔧 Размеры файлов:

- **Видео**: до 100MB (рекомендуется сжимать)
- **Изображения**: до 10MB
- **Форматы видео**: MP4, WebM
- **Форматы изображений**: JPG, PNG, WebP

## 🆘 Если что-то не работает:

1. **Проверьте переменные** в Vercel Settings
2. **Убедитесь** что SQL скрипт выполнился без ошибок
3. **Проверьте** что email в policies совпадает с вашим
4. **Перезапустите деплой** в Vercel

---

**После выполнения всех шагов админка будет полностью функциональна!** 🎉
